import string
import random
def generator_password(count, length):
    all_char = string.ascii_letters+string.digits+string.punctuation
    passwords = [''.join(random.choice(all_char) for _ in range(length)) for _ in range(count)]
    return passwords


print(generator_password(5, 10))
